import{mkdirSync}from"node:fs";import{spawnSync}from"node:child_process";import{join}from"node:path";const databaseUrl=process.env.DATABASE_URL;if(!databaseUrl)throw new Error("DATABASE_URL is required.");const url=new URL(databaseUrl),directory=process.env.WONFLOW_BACKUP_DIRECTORY??join(process.cwd(),".wonflow-backups");mkdirSync(directory,{recursive:true});const output=join(directory,`wonflow-${new Date().toISOString().replace(/[:.]/g,"-")}.dump`),result=spawnSync("pg_dump",["--format=custom","--no-owner","--no-privileges","--host",url.hostname,"--port",url.port||"5432","--username",decodeURIComponent(url.username),"--file",output,url.pathname.slice(1)],{stdio:"inherit",env:{...process.env,PGPASSWORD:decodeURIComponent(url.password)}});if(result.status!==0)process.exitCode=result.status??1;else console.log(`Backup created: ${output}`);
