import Link from "next/link";
import { WonFlowEmptyState } from "@/components/feedback/async-data-state";
import { PatientAppointmentCard } from "./patient-appointment-card";
import type { PatientAppointmentCardView, PatientAppointmentsScreenView } from "./patient-appointments-model";
function Section({ heading, description, appointments, emptyTitle, emptyDescription }: { heading: string; description: string; appointments: readonly PatientAppointmentCardView[]; emptyTitle: string; emptyDescription: string }) { return <section aria-labelledby={`${heading.toLowerCase()}-appointments-heading`} className="mt-8"><div><div className="flex items-center gap-2"><h2 id={`${heading.toLowerCase()}-appointments-heading`} className="text-xl font-semibold text-slate-950">{heading}</h2><span className="rounded-full bg-slate-100 px-2 py-0.5 text-xs font-semibold text-slate-700">{appointments.length}</span></div><p className="mt-1 text-sm leading-6 text-slate-600">{description}</p></div>{appointments.length ? <ul className="mt-4 grid list-none gap-4 p-0 lg:grid-cols-2">{appointments.map((appointment) => <li key={appointment.id}><PatientAppointmentCard appointment={appointment} /></li>)}</ul> : <div className="mt-4"><WonFlowEmptyState title={emptyTitle} description={emptyDescription} /></div>}</section>; }
export function PatientAppointmentsScreen({ bookingHref, upcomingAppointments, previousAppointments }: PatientAppointmentsScreenView) { const empty = !upcomingAppointments.length && !previousAppointments.length; return <main className="mx-auto w-full max-w-5xl px-4 py-6 sm:px-6 sm:py-8 lg:px-8"><header className="rounded-3xl border border-blue-100 bg-gradient-to-br from-blue-50 via-white to-violet-50 p-5 sm:p-7"><p className="text-sm font-semibold text-blue-700">Patient portal</p><div className="flex flex-col gap-4 sm:flex-row sm:items-start sm:justify-between"><div><h1 className="mt-1 text-3xl font-bold tracking-tight text-slate-950">Appointments</h1><p className="mt-3 text-sm leading-6 text-slate-600 sm:text-base">Review your upcoming visits and previous appointment history.</p></div>{bookingHref ? <Link href={bookingHref} className="inline-flex min-h-11 items-center justify-center rounded-xl bg-blue-700 px-4 py-2.5 text-sm font-semibold text-white">Book appointment</Link> : null}</div></header>{empty ? <div className="mt-8"><WonFlowEmptyState title="No appointments yet" description={bookingHref ? "You do not have any appointments yet. Book a visit when you are ready." : "Scheduled and previous visits will appear here."} /></div> : <><Section heading="Upcoming" description="Visits that are scheduled or currently active." appointments={upcomingAppointments} emptyTitle="No upcoming appointments" emptyDescription="You do not currently have any scheduled visits." /><Section heading="Previous" description="Completed, cancelled, or otherwise inactive visits." appointments={previousAppointments} emptyTitle="No previous appointments" emptyDescription="Completed or inactive visits will appear here." /></>}</main>; }
