import Link from "next/link";
import type { PracticeMoney } from "@wonflow/contracts";
import { formatPracticeMoney } from "./practice-money-format";
export interface PatientAppointmentPaymentModel { appointmentId:string;serviceName:string;amount:PracticeMoney;providerOptions:{id:string;label:string}[];backHref:string }
export function PatientAppointmentPaymentScreen({model}:{model:PatientAppointmentPaymentModel}){return <main className="mx-auto max-w-xl px-4 py-8" id="main-content"><Link className="text-sm font-black text-blue-700" href={model.backHref}>← Appointment details</Link><section className="mt-5 rounded-2xl border border-slate-200 bg-white p-5"><p className="text-xs font-black uppercase text-slate-500">Secure appointment payment</p><h1 className="mt-2 text-2xl font-black">{model.serviceName}</h1><p className="mt-3 text-lg font-black">{formatPracticeMoney(model.amount)}</p><p className="mt-4 text-sm font-semibold text-slate-600">Choose an available provider. The amount and eligibility are loaded from the server and cannot be changed here.</p>{model.providerOptions.length===0?<p className="mt-5 rounded-xl border border-amber-200 bg-amber-50 p-4 text-sm font-bold text-amber-900">No eligible payment provider is currently available.</p>:<div className="mt-5 space-y-2">{model.providerOptions.map((provider)=><button className="min-h-11 w-full rounded-xl bg-slate-950 px-4 text-sm font-black text-white" key={provider.id} type="button">Continue with {provider.label}</button>)}</div>}</section></main>}
