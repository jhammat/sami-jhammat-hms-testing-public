import Link from "next/link";
import type { PatientAppointmentCardView, PatientAppointmentStatusTone } from "./patient-appointments-model";
export function PatientAppointmentCard({ appointment }: { appointment: PatientAppointmentCardView }) {
  const tones: Record<PatientAppointmentStatusTone, string> = { neutral: "border-slate-200 bg-slate-100 text-slate-700", info: "border-blue-200 bg-blue-50 text-blue-700", positive: "border-emerald-200 bg-emerald-50 text-emerald-700", warning: "border-amber-200 bg-amber-50 text-amber-800", negative: "border-red-200 bg-red-50 text-red-700" };
  return <article className="overflow-hidden rounded-2xl border border-slate-200 bg-white shadow-sm"><div className="p-4 sm:p-5"><div className="flex items-start justify-between gap-3"><div><h3 className="text-base font-semibold text-slate-950">{appointment.serviceName}</h3><time dateTime={appointment.startsAt} className="mt-1 block text-sm font-medium text-blue-700">{appointment.dateLabel} · {appointment.timeLabel}</time><p className="mt-1 text-xs text-slate-500">{appointment.timeZoneLabel}</p></div><span className={`shrink-0 rounded-full border px-2.5 py-1 text-xs font-semibold ${tones[appointment.status.tone]}`}>{appointment.status.label}</span></div><dl className="mt-5 grid gap-4 sm:grid-cols-2">{appointment.clinicianName ? <div><dt className="text-xs font-medium uppercase tracking-wide text-slate-500">Clinician</dt><dd className="mt-1 text-sm text-slate-800">{appointment.clinicianName}</dd></div> : null}{appointment.locationName ? <div><dt className="text-xs font-medium uppercase tracking-wide text-slate-500">Location</dt><dd className="mt-1 text-sm text-slate-800">{appointment.locationName}</dd></div> : null}{appointment.visitFormatLabel ? <div><dt className="text-xs font-medium uppercase tracking-wide text-slate-500">Visit type</dt><dd className="mt-1 text-sm text-slate-800">{appointment.visitFormatLabel}</dd></div> : null}</dl>{appointment.preparationSummary ? <div className="mt-5 rounded-xl border border-blue-100 bg-blue-50/70 p-3.5"><h4 className="text-sm font-semibold text-slate-900">Before your appointment</h4><p className="mt-1 text-sm leading-6 text-slate-700">{appointment.preparationSummary}</p></div> : null}</div>{appointment.detailsHref ? <div className="border-t border-slate-100 bg-slate-50/70 p-3"><Link className="inline-flex min-h-11 w-full items-center justify-center rounded-xl px-4 py-2.5 text-sm font-semibold text-blue-700 hover:bg-blue-50 focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-blue-500" href={appointment.detailsHref}>View appointment details</Link></div> : null}</article>;
}
