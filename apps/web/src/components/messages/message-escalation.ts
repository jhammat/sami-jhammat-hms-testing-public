import type{IsoDateTime}from"@wonflow/contracts";export interface PracticeMessageEscalationState{responseDueAt?:IsoDateTime;overdue:boolean;overdueMinutes:number;ageMinutes:number;urgency:"normal"|"approaching"|"overdue"}export function calculatePracticeMessageEscalationState(createdAt:IsoDateTime,responseCommitmentMinutes:number,now:IsoDateTime,firstPracticeReplyAt?:IsoDateTime):PracticeMessageEscalationState{const start=Date.parse(createdAt),current=Date.parse(now),duration=Math.max(0,responseCommitmentMinutes)*60000,due=new Date(start+duration).toISOString() as IsoDateTime,age=Math.max(0,Math.floor((current-start)/60000));if(firstPracticeReplyAt)return{responseDueAt:due,overdue:false,overdueMinutes:0,ageMinutes:age,urgency:"normal"};const late=current-(start+duration),overdue=late>0,remaining=Math.floor((start+duration-current)/60000);return{responseDueAt:due,overdue,overdueMinutes:overdue?Math.floor(late/60000):0,ageMinutes:age,urgency:overdue?"overdue":remaining<=Math.max(15,Math.floor(responseCommitmentMinutes*.2))?"approaching":"normal"}}
