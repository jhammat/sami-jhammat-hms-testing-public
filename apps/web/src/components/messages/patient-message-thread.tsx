import type{PracticeMessageThreadProjection}from"@wonflow/mock-data";export function PatientMessageThread({thread}:{thread:PracticeMessageThreadProjection}){return <article className="rounded-2xl border bg-white p-4"><div className="flex items-center justify-between gap-3"><h2 className="font-black">{thread.subject}</h2><span className="rounded-full bg-slate-100 px-2 py-1 text-[10px] font-black uppercase">{thread.status.replaceAll("-"," ")}</span></div><p className="mt-1 text-xs text-slate-500">{thread.categoryLabel}</p><div className="mt-4 space-y-3">{thread.messages.map((message)=><div className={`rounded-xl p-3 ${message.authorType==="patient"?"ml-6 bg-indigo-50":"mr-6 bg-slate-100"}`} key={message.id}><p className="text-xs font-black">{message.authorDisplayName}</p><p className="mt-1 whitespace-pre-wrap text-sm">{message.body}</p><p className="mt-2 text-[10px] uppercase text-slate-500">{message.deliveryStatus.replaceAll("-"," ")}</p></div>)}</div></article>}
