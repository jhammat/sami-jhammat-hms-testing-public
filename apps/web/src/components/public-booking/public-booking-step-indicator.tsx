import type{PublicBookingStep}from"./public-booking-model";const steps:PublicBookingStep[]=["location","service","clinician","slot","review","account"];export function PublicBookingStepIndicator({current}:{current:PublicBookingStep}){const active=steps.indexOf(current);return <ol aria-label="Booking progress" className="grid grid-cols-3 gap-2 sm:grid-cols-6">{steps.map((step,index)=><li aria-current={step===current?"step":undefined} className={`rounded-xl px-2 py-2 text-center text-[10px] font-black uppercase ${index<=active?"bg-indigo-700 text-white":"bg-slate-100 text-slate-500"}`} key={step}>{step}</li>)}</ol>}
