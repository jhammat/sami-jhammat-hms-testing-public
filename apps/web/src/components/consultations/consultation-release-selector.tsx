import type{PracticeConsultationReleaseSection}from"@wonflow/mock-data";import{PRACTICE_CONSULTATION_RELEASE_SECTIONS}from"./consultation-completion-model";export function ConsultationReleaseSelector({value,onChange,disabled}:{value:readonly PracticeConsultationReleaseSection[];onChange(value:PracticeConsultationReleaseSection[]):void;disabled?:boolean}){return <fieldset disabled={disabled} className="rounded-2xl border p-4"><legend className="px-2 text-sm font-black">Release to patient</legend><p className="text-xs font-semibold text-slate-600">Select each section explicitly. Signing alone does not release content.</p><div className="mt-3 space-y-2">{PRACTICE_CONSULTATION_RELEASE_SECTIONS.map((item)=><label className="flex gap-3 rounded-xl border p-3" key={item.code}><input checked={value.includes(item.code)} onChange={(event)=>onChange(event.target.checked?[...value,item.code]:value.filter((code)=>code!==item.code))} type="checkbox"/><span><span className="block text-sm font-black">{item.label}</span><span className="text-xs text-slate-600">{item.description}</span></span></label>)}</div></fieldset>}
