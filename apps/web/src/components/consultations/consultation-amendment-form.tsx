"use client";import{useState}from"react";export function ConsultationAmendmentForm({onSubmit}:{onSubmit(input:{reason:string;content:string}):Promise<void>}){const[reason,setReason]=useState("");const[content,setContent]=useState("");return <form className="rounded-2xl border bg-white p-4" onSubmit={(event)=>{event.preventDefault();if(reason.trim()&&content.trim())void onSubmit({reason,content})}}><h2 className="font-black">Add amendment</h2><p className="mt-1 text-xs text-slate-600">Signed content remains unchanged. This amendment is appended to the record.</p><input className="mt-4 min-h-11 w-full rounded-xl border px-3" onChange={(event)=>setReason(event.target.value)} placeholder="Reason" required value={reason}/><textarea className="mt-3 min-h-28 w-full rounded-xl border p-3" onChange={(event)=>setContent(event.target.value)} placeholder="Amendment" required value={content}/><button className="mt-3 min-h-11 w-full rounded-xl bg-slate-950 px-4 text-sm font-black text-white" type="submit">Add amendment</button></form>}
