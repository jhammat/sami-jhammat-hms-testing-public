import type{ReactNode}from"react";import{Bell,CalendarDays,FileText,HeartPulse}from"lucide-react";import{PhaseOneHero,PhaseOneMetricCard,PhaseOnePage}from"@/components/phase-one-design";
export function PatientProfessionalSurface({title,description,appointments,availableResults,documents,notifications,actions,children}:{title:string;description:string;appointments:number;availableResults:number;documents:number;notifications:number;actions?:ReactNode;children:ReactNode}){return <PhaseOnePage><PhaseOneHero actions={actions} description={description} eyebrow="My care" title={title}/><section className="grid gap-4 sm:grid-cols-2 xl:grid-cols-4"><PhaseOneMetricCard description="Upcoming scheduled visits" icon={<CalendarDays aria-hidden size={20}/>} label="Appointments" value={appointments}/><PhaseOneMetricCard description="Released results available to view" icon={<HeartPulse aria-hidden size={20}/>} label="Results" value={availableResults}/><PhaseOneMetricCard description="Documents shared with your account" icon={<FileText aria-hidden size={20}/>} label="Documents" value={documents}/><PhaseOneMetricCard description="Unread account notifications" icon={<Bell aria-hidden size={20}/>} label="Notifications" value={notifications}/></section>{children}</PhaseOnePage>}
