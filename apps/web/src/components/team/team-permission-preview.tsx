import { CheckCircle2, ShieldAlert } from "lucide-react";
import type { PracticePrivilege } from "@wonflow/contracts";
import { getPracticePrivilegeDescription } from "./team-management-safety";
export function TeamPermissionPreview({privileges,warning,title="What this member can do"}:{privileges:readonly PracticePrivilege[];warning?:string;title?:string}){return <section aria-labelledby="team-permission-preview-heading" className="rounded-2xl border border-cyan-200 bg-cyan-50 p-4"><h3 id="team-permission-preview-heading" className="text-sm font-black">{title}</h3>{privileges.length?<ul className="mt-4 grid gap-2 lg:grid-cols-2">{privileges.map((privilege)=><li key={privilege} className="flex gap-2 rounded-xl border bg-white p-3"><CheckCircle2 aria-hidden className="shrink-0 text-emerald-600" size={15}/><div><p className="text-xs font-black">{privilege}</p><p className="mt-1 text-xs text-slate-600">{getPracticePrivilegeDescription(privilege)}</p></div></li>)}</ul>:<p className="mt-4 text-xs font-bold text-amber-900">This member currently has no operational privileges.</p>}{warning?<p role="alert" className="mt-4 flex gap-2 rounded-xl border border-rose-200 bg-rose-50 p-3 text-xs font-bold text-rose-900"><ShieldAlert aria-hidden size={16}/>{warning}</p>:null}</section>}
