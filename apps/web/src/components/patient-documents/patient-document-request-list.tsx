import type { PatientDocumentRequestItem } from "@wonflow/mock-data";
export function PatientDocumentRequestList({requests,onFulfill}:{requests:readonly PatientDocumentRequestItem[];onFulfill(id:string):void}) { const open=requests.filter((request)=>request.status==="open");if(open.length===0)return null;return <section aria-labelledby="document-requests-heading" className="rounded-2xl border border-amber-200 bg-amber-50 p-4"><h2 id="document-requests-heading" className="font-black text-amber-950">Requested by your care team</h2><div className="mt-3 space-y-3">{open.map((request)=><article className="rounded-xl bg-white p-4" key={request.id}><h3 className="text-sm font-black">{request.title}</h3>{request.instructions?<p className="mt-1 text-xs font-semibold text-slate-600">{request.instructions}</p>:null}<button className="mt-3 min-h-11 rounded-xl bg-amber-800 px-4 text-sm font-black text-white" onClick={()=>onFulfill(request.id)} type="button">Upload requested document</button></article>)}</div></section> }
