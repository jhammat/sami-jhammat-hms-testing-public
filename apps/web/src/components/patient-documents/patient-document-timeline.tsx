import type { PatientDocumentTimelineItem } from "@wonflow/mock-data";
import { FileText,LoaderCircle } from "lucide-react";
export function PatientDocumentTimeline({documents,onOpen,openingDocumentId}:{documents:readonly PatientDocumentTimelineItem[];onOpen(id:string):void;openingDocumentId?:string}) { return <section aria-labelledby="document-timeline-heading" className="rounded-2xl border border-slate-200 bg-white p-4"><h2 id="document-timeline-heading" className="text-base font-black">Document timeline</h2>{documents.length===0?<p className="mt-4 rounded-xl border border-dashed p-5 text-sm text-slate-600">No patient documents are available.</p>:<div className="mt-4 space-y-3">{documents.map((document)=><article className="flex items-start gap-3 rounded-xl border border-slate-200 p-4" key={document.id}><FileText aria-hidden className="text-indigo-700"/><div className="min-w-0 flex-1"><h3 className="truncate text-sm font-black">{document.title}</h3><p className="mt-1 text-xs text-slate-600">{document.categoryLabel} · {document.clinicalDate} · {document.scanStatus}</p></div><button className="min-h-11 rounded-xl bg-slate-950 px-3 text-xs font-black text-white disabled:opacity-50" disabled={!document.openable||openingDocumentId===document.id} onClick={()=>onOpen(document.id)} type="button">{openingDocumentId===document.id?<LoaderCircle aria-hidden className="animate-spin" size={16}/>:"Open"}</button></article>)}</div>}</section> }
