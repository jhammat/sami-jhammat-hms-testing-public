import type { PatientDocumentUploadPolicy } from "@wonflow/mock-data";
export interface PatientDocumentFileValidationResult { valid:boolean; errors:string[] }
export function formatPatientDocumentFileSize(bytes:number) { if(bytes<1024)return `${bytes} B`; const kb=bytes/1024;if(kb<1024)return `${kb.toFixed(1)} KB`;const mb=kb/1024;if(mb<1024)return `${mb.toFixed(1)} MB`;return `${(mb/1024).toFixed(2)} GB`; }
export function validatePatientDocumentFile(file:File,policy:PatientDocumentUploadPolicy):PatientDocumentFileValidationResult { const errors:string[]=[];if(file.size<=0)errors.push("The selected file is empty.");if(file.size>policy.maximumFileSizeBytes)errors.push(`The selected file is larger than ${formatPatientDocumentFileSize(policy.maximumFileSizeBytes)}.`);if(!policy.acceptedMimeTypes.map((type)=>type.trim().toLowerCase()).includes(file.type.trim().toLowerCase()))errors.push("The selected file type is not permitted.");return {valid:errors.length===0,errors}; }
