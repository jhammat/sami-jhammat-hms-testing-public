import type{ReactNode}from"react";import{CalendarDays,ClipboardPlus,Stethoscope,Users}from"lucide-react";import{PhaseOneHero,PhaseOneMetricCard,PhaseOnePage}from"@/components/phase-one-design";
export interface DoctorWorkspaceMetrics{appointments:number;waiting:number;inProgress:number;completed:number}
export function DoctorProfessionalSurface({title,description,metrics,actions,children}:{title:string;description:string;metrics:DoctorWorkspaceMetrics;actions?:ReactNode;children:ReactNode}){return <PhaseOnePage><PhaseOneHero actions={actions} description={description} eyebrow="Doctor workspace" title={title}/><section className="grid gap-4 sm:grid-cols-2 xl:grid-cols-4"><PhaseOneMetricCard description="Scheduled for the selected day" icon={<CalendarDays aria-hidden size={20}/>} label="Appointments" value={metrics.appointments}/><PhaseOneMetricCard description="Patients waiting in the current branch" icon={<Users aria-hidden size={20}/>} label="Waiting" value={metrics.waiting}/><PhaseOneMetricCard description="Consultations currently open" icon={<Stethoscope aria-hidden size={20}/>} label="In progress" value={metrics.inProgress}/><PhaseOneMetricCard description="Consultations completed today" icon={<ClipboardPlus aria-hidden size={20}/>} label="Completed" value={metrics.completed}/></section>{children}</PhaseOnePage>}
