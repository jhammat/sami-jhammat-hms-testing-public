"use client";
import Link from "next/link";
import { ArrowRight, Banknote, Building2, MapPin, ShieldCheck } from "lucide-react";
import { usePracticeLocation } from "@/components/shell";
import { useDoctorPortalContext } from "./doctor-portal-shell";
export function DoctorConsultationFeeCard() { const { doctor } = useDoctorPortalContext(); const { selectedLocation } = usePracticeLocation(); if (!doctor) return null; return <section aria-labelledby="doctor-fee-card-heading" className="overflow-hidden rounded-2xl border border-violet-200 bg-white shadow-lg"><header className="bg-gradient-to-r from-[#07142f] via-indigo-950 to-violet-800 px-4 py-4 text-white"><div className="flex items-start gap-3"><Banknote aria-hidden size={28}/><div><p className="text-[9px] font-black uppercase tracking-widest text-violet-200">Canonical pricing</p><h2 id="doctor-fee-card-heading" className="mt-1 text-base font-black">Consultation fees</h2><p className="text-xs text-indigo-200">{doctor.displayName}</p></div></div></header><div className="p-4"><div className="flex gap-3 rounded-xl border border-indigo-100 bg-indigo-50 p-4"><ShieldCheck aria-hidden className="shrink-0 text-indigo-600" size={18}/><p className="text-xs leading-5 text-indigo-950">The hospital controls whether consultation fees are maintained by doctors or administrators. The same backend record is used by appointments and billing.</p></div><dl className="mt-4 grid gap-3 sm:grid-cols-2"><div className="rounded-xl bg-slate-50 p-3"><dt className="flex items-center gap-2 text-xs font-black uppercase text-slate-500"><Building2 aria-hidden size={14}/>Clinician</dt><dd className="mt-2 break-words text-sm font-black">{doctor.displayName}</dd></div><div className="rounded-xl bg-slate-50 p-3"><dt className="flex items-center gap-2 text-xs font-black uppercase text-slate-500"><MapPin aria-hidden size={14}/>Active location</dt><dd className="mt-2 break-words text-sm font-black">{selectedLocation?.name ?? "All locations"}</dd></div></dl><Link className="mt-4 inline-flex min-h-10 items-center gap-2 rounded-xl bg-indigo-600 px-4 text-xs font-black text-white" href="/doctor/fees">Manage fees & services<ArrowRight aria-hidden size={15}/></Link></div></section>; }
