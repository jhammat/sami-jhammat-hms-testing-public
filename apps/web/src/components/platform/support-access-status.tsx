import { Clock3, ShieldCheck, ShieldX } from "lucide-react";
import { isPlatformSupportAccessActive } from "./platform-tenant-safety";
export function SupportAccessStatus({status,expiresAt,permissions,now}:{status:string;expiresAt:string;permissions:readonly string[];now:string}){const active=isPlatformSupportAccessActive({status,expiresAt},now);return <section className={`rounded-2xl border p-4 ${active?"border-emerald-200 bg-emerald-50":"border-slate-200 bg-slate-50"}`}><h3 className="flex items-center gap-2 font-black">{active?<ShieldCheck aria-hidden size={18}/>:<ShieldX aria-hidden size={18}/>}Support access {active?"active":"inactive"}</h3><p className="mt-2 flex items-center gap-2 text-xs"><Clock3 aria-hidden size={14}/>Expires {expiresAt}</p><p className="mt-2 break-words text-xs">Permissions: {permissions.join(", ")||"None"}</p></section>}
