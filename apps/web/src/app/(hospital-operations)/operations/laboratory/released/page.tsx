import { DiagnosticsWorkspace } from "@/components/diagnostics/diagnostics-workspace";

export default function LaboratoryReleasedPage() {
  return <DiagnosticsWorkspace type="LABORATORY" view="released" />;
}
