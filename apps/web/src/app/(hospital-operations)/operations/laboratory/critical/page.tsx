import { DiagnosticsWorkspace } from "@/components/diagnostics/diagnostics-workspace";

export default function LaboratoryCriticalPage() {
  return <DiagnosticsWorkspace type="LABORATORY" view="critical" />;
}
