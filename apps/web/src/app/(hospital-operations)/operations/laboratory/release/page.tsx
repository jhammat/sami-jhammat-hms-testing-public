import { DiagnosticsWorkspace } from "@/components/diagnostics/diagnostics-workspace";

export default function LaboratoryReleasePage() {
  return <DiagnosticsWorkspace type="LABORATORY" view="release" />;
}
