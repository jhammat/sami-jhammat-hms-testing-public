import { DiagnosticsWorkspace } from "@/components/diagnostics/diagnostics-workspace";

export default function LaboratoryProcessingPage() {
  return <DiagnosticsWorkspace type="LABORATORY" view="processing" />;
}
