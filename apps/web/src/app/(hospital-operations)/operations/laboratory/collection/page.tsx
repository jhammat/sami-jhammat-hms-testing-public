import { DiagnosticsWorkspace } from "@/components/diagnostics/diagnostics-workspace";

export default function LaboratoryCollectionPage() {
  return <DiagnosticsWorkspace type="LABORATORY" view="collection" />;
}
