import { DiagnosticsWorkspace } from "@/components/diagnostics/diagnostics-workspace";

export default function RadiologyWorklistPage() {
  return <DiagnosticsWorkspace type="RADIOLOGY" view="all" />;
}
