import { DiagnosticsWorkspace } from "@/components/diagnostics/diagnostics-workspace";

export default function RadiologyCriticalPage() {
  return <DiagnosticsWorkspace type="RADIOLOGY" view="critical" />;
}
