import { DiagnosticsWorkspace } from "@/components/diagnostics/diagnostics-workspace";

export default function RadiologyReleasePage() {
  return <DiagnosticsWorkspace type="RADIOLOGY" view="release" />;
}
