import { DiagnosticsWorkspace } from "@/components/diagnostics/diagnostics-workspace";

export default function RadiologyProcessingPage() {
  return <DiagnosticsWorkspace type="RADIOLOGY" view="processing" />;
}
