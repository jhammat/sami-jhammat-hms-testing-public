import type{Metadata}from"next";import{notFound}from"next/navigation";import{PublicBookingScreen}from"@/components/public-booking";import{loadPublicBookingEntry}from"@/components/public-booking/public-booking.server";export const metadata:Metadata={title:"Book an appointment | WonFlow",robots:{index:false,follow:false}};export const dynamic="force-dynamic";export const revalidate=0;export default async function Page({params}:{params:Promise<{organizationId:string}>}){const{organizationId}=await params;const model=await loadPublicBookingEntry(organizationId);if(!model)notFound();return <PublicBookingScreen model={model}/>}
