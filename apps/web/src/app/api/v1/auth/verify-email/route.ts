import{NextResponse}from"next/server";import{database}from"@wonflow/database";import{consumeOneTimeToken}from"@/lib/auth/one-time-token";
export async function POST(request:Request){const body=await request.json().catch(()=>null)as{token?:string}|null;if(!body?.token)return NextResponse.json({error:"Token is required."},{status:400});const token=await consumeOneTimeToken(body.token,"EMAIL_VERIFICATION");if(!token)return NextResponse.json({error:"The verification link is invalid or expired."},{status:400});await database.$transaction(async tx=>{await tx.identity.update({where:{id:token.identityId},data:{emailVerifiedAt:new Date()}});await tx.auditEvent.create({data:{tenantId:token.tenantId,requestId:crypto.randomUUID(),action:"identity.email.verified",entityType:"identity",entityId:token.identityId,severity:"INFORMATION",sourceApplication:"web"}})});return NextResponse.json({ok:true})}
