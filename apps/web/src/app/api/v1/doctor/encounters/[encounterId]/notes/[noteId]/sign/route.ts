import{NextResponse}from"next/server";import{requireRequestContext}from"@/lib/auth/permission-service";import{doctorService as s}from"@/server/doctor/doctor-service";import{handleApiRoute}from"@/server/http/route-handler";export function POST(_r:Request,c:{params:Promise<{encounterId:string;noteId:string}>}){return handleApiRoute(async()=>{const p=await c.params;return NextResponse.json({note:await s.signNote(await requireRequestContext(),p.encounterId,p.noteId)})})}
