import{NextResponse}from"next/server";import{requireRequestContext}from"@/lib/auth/permission-service";import{doctorService as s}from"@/server/doctor/doctor-service";import{handleApiRoute}from"@/server/http/route-handler";export function GET(r:Request){return handleApiRoute(async()=>NextResponse.json({dashboard:await s.getDashboard(await requireRequestContext(),new URL(r.url).searchParams.get("date")??new Date().toISOString().slice(0,10))}))}
