import{describe,expect,it}from"vitest";import{requireBranchId,requirePermission}from"@wonflow/contracts";describe("request context",()=>{const context={scope:"tenant"as const,requestId:"request-1",userId:"membership-1",identityId:"identity-1",membershipId:"membership-1",sessionId:"session-1",workspace:"reception",locale:"en",timezone:"Asia/Karachi",currencyCode:"PKR",permissionCodes:["patients.read"],sourceApplication:"web"as const,tenantId:"tenant-1",organizationId:"organization-1",branchId:"branch-1"};it("returns the authorized branch",()=>expect(requireBranchId(context)).toBe("branch-1"));it("rejects a missing permission",()=>expect(()=>requirePermission(context,"patients.manage")).toThrow())});
