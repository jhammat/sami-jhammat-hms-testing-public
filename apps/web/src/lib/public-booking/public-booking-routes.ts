export const PUBLIC_BOOKING_ROOT_PATH="/book";export function createPublicBookingPath(id:string){return `${PUBLIC_BOOKING_ROOT_PATH}/${encodeURIComponent(id)}`}export function createPublicBookingSlotsApiPath(id:string,input:{offeringId:string;consultationMode:string;clinicianTeamMemberId?:string}){const query=new URLSearchParams({offeringId:input.offeringId,consultationMode:input.consultationMode});if(input.clinicianTeamMemberId)query.set("clinicianTeamMemberId",input.clinicianTeamMemberId);return `/api/public-booking/${encodeURIComponent(id)}/slots?${query}`}export function createPublicBookingResumeApiPath(id:string){return `/api/public-booking/${encodeURIComponent(id)}/resume`}
